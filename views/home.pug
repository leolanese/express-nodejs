extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action="/", method="POST")
    input(type="email", placeholder="placeholder email", name="email")
    button(type="submit") Submit